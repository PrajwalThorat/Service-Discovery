#Add properties to tell the built-in Eureka Client not to register with itself, as this application is acting as a server.

eureka.client.register-with-eureka = false
eureka.client.fetch-registry = false